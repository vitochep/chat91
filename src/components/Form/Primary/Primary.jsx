import styled from 'styled-components';
import Form from '../Form.jsx';

const Primary = styled(Form)`
	padding: 20px 30px;
    flex-direction: row;
    align-items: center;
    width: 100%;
`;

export default Primary;
