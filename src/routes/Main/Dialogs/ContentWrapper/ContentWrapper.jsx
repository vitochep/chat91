import styled from 'styled-components';
import {Centered} from 'components/Block';

const ContentWrapper = styled(Centered)`
		padding: 0 20px;
		height: calc(100vh - 108px);
`;

export default ContentWrapper;
